<manifest package="com.valxu.recycler.photos"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppTheme">

        <activity
            android:name=".ui.activity.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activity.GalleryActivity"
            android:label="@string/gallery_activity"
            android:screenOrientation="portrait"
            />

        <activity
            android:name=".ui.activity.GalleryGroupActivity"
            android:label="@string/gallery_group_activity"
            android:screenOrientation="portrait"/>

    </application>

</manifest>
